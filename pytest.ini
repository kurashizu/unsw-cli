[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    network: tests that require real network access to UNSW servers
    auth: tests that require real authentication credentials
asyncio_mode = auto
